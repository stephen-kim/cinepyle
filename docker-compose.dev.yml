services:
  cinepyle:
    build: .
    pull_policy: always
    env_file:
      - .env
    ports:
      - "${DASHBOARD_PORT:-3847}:${DASHBOARD_PORT:-3847}"
    volumes:
      - cinepyle-data:/app/data
    restart: unless-stopped

volumes:
  cinepyle-data:
