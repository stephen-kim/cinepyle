services:
  cinepyle:
    build: .
    env_file:
      - .env
    ports:
      - "${DASHBOARD_PORT:-8080}:8080"
    volumes:
      - cinepyle-data:/app/data
    restart: unless-stopped

volumes:
  cinepyle-data:
